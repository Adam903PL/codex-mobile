# DevLink CLI

Lokalne CLI łączy komputer użytkownika z backendem DevLink i uruchamia `Codex CLI` w zarejestrowanych projektach.

```bash
pip install -e .
devlink doctor
devlink pair --code AB12CD --name "Laptop"
devlink projects add --path C:\repo --name "My Project"
devlink connect
```
